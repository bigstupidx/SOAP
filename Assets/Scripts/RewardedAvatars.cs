using UnityEngine;
using System.Collections;

public class RewardedAvatars : MonoBehaviour {


    public readonly string[] rewarded_avatar_name_list = { "default_avatar, default_tail, cyborg_avatar, cyborg_tail" };

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
